# Issue Tracker Configuration

The issue tracker for this repository is GitHub Issues.

External pull requests (PRs) are considered a request surface and will be triaged alongside issues. This means that when the `/triage` skill is used, it will include external PRs in the triage queue and apply the configured labels and states to them. In-flight PRs from collaborators are excluded from this process.

**CLI Tool**: `gh` (GitHub CLI)
